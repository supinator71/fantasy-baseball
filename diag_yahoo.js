const yahooService = require('./server/services/yahooService');

async function testSettings() {
  try {
    // Check if we can fetch settings for a mocked/fake league key, but we need an authenticated user.
    // I can read the source code in `server/routes/yahoo.js` instead.
  } catch(e) {}
}
